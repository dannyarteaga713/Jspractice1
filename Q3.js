const partyLocation = "GARDEN"; // if the location is the same
const shiftValue = 2; // if we use just a fixed number
let encryptedMessage = ""; // String "ICTFGP"  could be variable 
let decryptedMessage = ""; // String "GARDEN"   could be variable
let SameMessage = false; // Boolean     could be variable
